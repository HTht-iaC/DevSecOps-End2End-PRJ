variables:
  TERRAFORM_ACTION: apply
  TF_IN_AUTOMATION: "true"

before_script:
  - terraform --version
  - terraform init

stages:
  - validation
  - planning
  - action

Config files Validation_for_runner:
  stage: validation
  tags:
    - terraform
  rules:
    - changes:
        - terraform/ec2-gitlab-runner/**/*
      when: always
    - when: never
  script:
    - terraform validate

planning_for_runner:
  stage: planning
  tags:
    - terraform
  rules:
    - changes:
        - terraform/ec2-gitlab-runner/**/*
      when: always
    - when: never
  script:
    - terraform plan

apply_job_for_runner:
  stage: action
  tags:
    - terraform
  rules:
    - changes:
        - terraform/ec2-gitlab-runner/**/*
      when: always
    - when: never
  script:
    - if [[ "$TERRAFORM_ACTION" == "apply" ]]; then
        terraform apply -auto-approve
      fi
  when: manual
  only:
    variables:
      - $TERRAFORM_ACTION == "apply"

destroy_job_for_runner:
  stage: action
  tags:
    - terraform
  rules:
    - changes:
        - terraform/ec2-gitlab-runner/**/*
      when: always
    - when: never
  script:
    - if [[ "$TERRAFORM_ACTION" == "destroy" ]]; then
        terraform destroy -auto-approve
      fi
  when: manual
  only:
    variables:
      - $TERRAFORM_ACTION == "destroy"
